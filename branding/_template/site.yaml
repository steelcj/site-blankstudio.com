# site.yaml — client-owned site configuration (template default).
#
# scripts/branding/new-client.js copies this directory to branding/<client>/;
# edit the copy, not this file. build-brand.py reads the active client's
# site.yaml and generates src/_data/site.json. Run `npm run build:brand` after
# changing a value, and commit the regenerated site.json (never edit it by hand).
#
# `contact` holds the direct-contact facts the templates read. `social` is a
# map of accounts; the footer renders a link for each one present, so adding a
# network (github, facebook, linkedin, x, youtube, …) makes it appear with no
# template change. Each social entry is { url, label }.

# ── The verbal identity ──────────────────────────────────────────────────────
#
# The three fields below are what this organisation calls itself, and they are
# the only place any of it is written down. Everything that shows a name, a
# slogan or a boilerplate sentence reads them from here: the copyright line, the
# footer paragraph, a page with no meta description of its own, and the text
# alternative on the logo.
#
# The rule that keeps them honest is that nothing copies them. A slogan pasted
# into a locale bundle, or drawn into an SVG, is a second copy that this file
# can no longer correct — which is exactly how a rebranded site went on calling
# itself by its old name in the one string a screen reader reads aloud. If you
# find a name or a slogan written anywhere else, that place is the defect.
#
# See `assets/README.md` for what this means for a logo file specifically.

name: "Example Studio"                    # TODO: the name in the copyright line

# The slogan — the line beside the copyright. One string when the same words
# serve every language — most taglines are a brand asset, not a sentence to
# translate:
#
#   tagline: "Strategy. Design. Impact."
#
# …or a map keyed by locale when it should change per language:
#
#   tagline:
#     en-ca: "Technology, design, and infrastructure for greater agency."
#     fr-ca: "Technologie, design et infrastructure pour une plus grande agentivité."
#
# Omit it entirely and the footer falls back to the locale bundles.
tagline: "Strategy. Design. Impact."

# What this organisation does, in a sentence — the paragraph in the footer, and
# the sentence to reach for wherever a page has none of its own (a meta
# description, a card, a feed). In press terms this is the boilerplate.
#
# Takes the same two shapes as `tagline`: one string, or a map keyed by locale.
description: "Example Studio builds things for people who need them."

url: "https://www.example.com"            # TODO: the client's canonical site URL
publicationTimeZone: "America/Toronto"    # TODO: the client's publication timezone

# Optional — the loading screen: the logo and progress bar that cover the page
# on first paint, then fade. It is decorative, not a real progress indicator —
# it dismisses on a fixed timer once the page has loaded — so it is a brand
# choice, not a performance one. Defaults to true when omitted.
#
# Set false to drop it site-wide; the markup is then never emitted, so there is
# no hidden overlay left behind. A single page can go the other way with
# `showLoader: true` (or false) in its own front matter, which wins over this.
showLoader: false

# Optional — override the logo/favicon paths (any filename or format: SVG, PNG,
# …). Defaults to the .png slots below if omitted. Files live in this client's
# assets/ directory.
#
# `alt` is the mark's text alternative — what a screen reader speaks for the
# logo link, and what a reader sees when the image does not load. Leave it out
# and the header and footer build the string themselves, from `name` above and
# the locale bundle's pattern: "Example Studio home" in English, "Accueil
# Example Studio" in French. That is the right answer for a mark that reads as
# the organisation's name, which is nearly all of them.
#
# Declare it only when the mark carries words the name does not — an
# abbreviation, a former name, a mark with a slogan drawn into it. Takes the
# same two shapes as `tagline`: one string, or a map keyed by locale.
#
#   alt: "ES — Example Studio"
#
#   alt:
#     en-ca: "Example Studio, home"
#     fr-ca: "Example Studio, accueil"
# logos:
#   dark: "/assets/logo-lockup-dark.png"     # dark mark, light backgrounds
#   light: "/assets/logo-lockup.png"         # light mark, dark footer
#   favicon: "/assets/favicon.png"
#   alt: "Example Studio"

# Optional — the primary menu. The menu items themselves are the sections in
# this client's content tree (src/content/<locale>/ under this directory), so a
# new section is a new directory, not a template edit. This block only says
# where the designed landing pages sit around them, and in what order the known
# sections appear. Anything not listed in `order` is appended, alphabetically.
# nav:
#   before: ["home"]                         # pages pinned ahead of the sections
#   after: ["contact"]                       # …and after them
#   order: ["about", "services", "blog"]     # display order of the sections

contact:
  email: "hello@example.com"              # TODO

  # Optional — a phone number, if this client publishes one. Uncomment both
  # lines: the templates show `phoneDisplay` and dial `phoneHref`, so they have
  # to describe the same number in the two forms a page needs it.
  # phoneDisplay: "+1 416 555 0142"       # as a reader reads it
  # phoneHref: "tel:+14165550142"         # as a phone dials it, no spaces

social:
  # Social accounts. Every entry present here becomes a footer link, in the
  # order they appear, so a network is added by uncommenting it — no template
  # change. `url` is where the link goes; `label` is what it reads.
  #
  # whatsapp has a second life: build-brand also exposes its url as
  # site.contact.whatsapp, for the templates that name that key directly.

  # instagram:
  #   url: "https://instagram.com/example"
  #   label: "@example"
  # facebook:
  #   url: "https://facebook.com/example"
  #   label: "Example on Facebook"
  # linkedin:
  #   url: "https://linkedin.com/company/example"
  #   label: "Example on LinkedIn"
  # x:
  #   url: "https://x.com/example"
  #   label: "@example"
  # youtube:
  #   url: "https://youtube.com/@example"
  #   label: "Example on YouTube"
  # github:
  #   url: "https://github.com/example"
  #   label: "github.com/example"
  # mastodon:
  #   url: "https://mastodon.social/@example"
  #   label: "@example@mastodon.social"
  # whatsapp:
  #   url: "https://wa.me/14165550142"
  #   label: "WhatsApp"
